bin.name = "cargo-add"
args = ["add", "this-should-not-show-up", "--path", "."]
status.code = 1
stdout = ""
stderr = """
      Adding this-should-not-show-up (local) to dependencies.
Could not edit `Cargo.toml`.

ERROR: Cannot add `cargo-list-test-fixture` as a dependency to itself
Command failed due to unhandled error: Cannot add `cargo-list-test-fixture` as a dependency to itself

"""
fs.sandbox = true

[env.add]
CARGO_IS_TEST="1"
